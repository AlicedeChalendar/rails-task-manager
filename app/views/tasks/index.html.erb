<h1>My tasks</h1>

<br>

<ul>
  <% @tasks.each do |task| %>
    <% if task.completed %>
    <p>✅ <%=link_to task.title, task_path(task.id)%></p>
    <% else %>
    <p>🔲 <%=link_to task.title, task_path(task.id)%></p>
    <% end %>
  <% end %>
</ul>

<%= link_to 'Create new', new_task_path, class: 'btn btn-primary btn-sm' %>
