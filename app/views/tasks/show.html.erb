<h2><%= @task.title %></h2>

<p><%= @task.details %></p>

<% if @task.completed %>
  <p> Completed? ✅</p>
  <% else %>
  <p> Completed? 🔲</p>
<% end %>


<br>

<div>
<%= link_to 'Edit task', edit_task_path(@task), class: 'btn btn-secondary btn-sm mb-2' %>
<%= link_to 'Delete task', delete_task_path(@task), class: 'btn btn-danger btn-sm mb-2', method: :delete, data: {confirm: "Are you sure?" } %>
</div>

<p><%= link_to "<< Back", tasks_path %></p>
