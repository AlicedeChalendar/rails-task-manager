<h2>Create a new task</h2>

<%= form_for(@task) do |f| %>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :details %>
    <%= f.text_field :details, class: 'form-control' %>
  </div>

  <div>
  <%= f.submit "Create task", class: 'btn btn-primary' %> or <%= link_to "Cancel", tasks_path %>
  </div>
<% end %>
